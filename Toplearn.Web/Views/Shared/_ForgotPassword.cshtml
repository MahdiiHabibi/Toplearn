@model Toplearn.Core.DTOs.Accounts.SendEmailHtmlViewModel

@{
	var xx = TempData["Url"];
	var x = TempData["Url"];
}
<h1>
	<a href="https://localhost:7208/">تاپ لرن</a>
</h1>
<h2>
	بازیابی رمز عبور حساب کاربری
</h2>

<h3>
	سلام @Model.FullName
</h3>

<p>
	جهت بازیابی رمز عبور حساب کاربری بر روی این لینک کلیک کنید
</p>
<a style="text-align:center;" href="@(Model.HostUrl)/Account/ResetPassword/@(Model.ActiveCode)?BackUrl=@(Model.BackUrl)">
	بازیابی رمزعبور
</a>
<p style="text-align:center;">
	@(Model.HostUrl)/Account/ResetPassword/@(Model.ActiveCode)?BackUrl=@(Model.BackUrl)
</p>